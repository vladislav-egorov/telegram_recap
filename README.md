# ⏪ Telegram Chat Rewind

**Telegram Chat Rewind** — это Python-инструмент, который превращает скучную историю переписки в стильный статистический отчет. Узнайте, кто был самым активным в этом году, какие слова вы использовали чаще всего и в какие дни чат "горел".

В отличие от сторонних ботов, этот скрипт работает **локально** на вашем компьютере. Ваша переписка никуда не отправляется.

## ✨ Возможности
* 📊 **Полная статистика:** Количество сообщений, участников, ссылок и медиа.
* 🏆 **Доска почета:** Топ участников по сообщениям, стикерам и картинкам.
* 🗣 **Словарный запас:** Умное облако часто используемых слов (с фильтрацией предлогов и мусора).
* 📅 **Хронология:** Определение самых активных дней и точного периода переписки.
* 🔒 **Приватность:** 100% безопасная обработка данных.

---

## 🚀 Установка

### 1. Клонирование и подготовка
Скачайте проект и откройте папку в терминале:
```bash
git clone [https://github.com/your-username/telegram-chat-rewind.git](https://github.com/your-username/telegram-chat-rewind.git)
cd telegram-chat-rewind
```

### 2. Создание виртуального окружения (рекомендуется)
Чтобы не засорять систему библиотеками:

**Windows:**
```bash
python -m venv .venv
.venv\Scripts\activate
```

**macOS / Linux:**
```bash
python3 -m venv .venv
source .venv/bin/activate
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

---

## 📥 Как получить данные (result.json)

Для работы скрипта нужна история чата в формате JSON. Это можно сделать только через **Telegram Desktop** (версия для компьютера).

1.  Откройте **Telegram Desktop** на компьютере.
2.  Зайдите в нужный чат или группу.
3.  Нажмите на **три точки (⋮)** в правом верхнем углу → **Экспорт истории чата** (Export chat history).
4.  В настройках экспорта:
    * **Галочки:** Снимите галочки с «Фотографии», «Видео» и других файлов (это ускорит процесс, скрипту нужны только метаданные).
    * **Формат:** Выберите **«Машиночитаемый JSON»** (Machine-readable JSON). Это самый важный шаг!
    * **Путь:** Запомните, куда скачивается файл.
5.  Нажмите **Экспортировать**.
6.  После завершения найдите файл (обычно он называется `result.json`) и **переместите его в папку с этим проектом**.

> **Важно:** Файл должен называться строго `result.json`. Если он называется иначе, переименуйте его.

---

## 🛠 Запуск

### Шаг 1: Анализ данных
Этот скрипт прочитает JSON, очистит текст от мусора и посчитает цифры.
```bash
python analyze_chat.py
```
*После успеха появится файл `chat_stats.json`.*

### Шаг 2: Генерация отчета
Этот скрипт превратит сухие цифры в красивый текстовый отчет.
```bash
python generate_report.py
```

### Шаг 3: Готово!
Откройте файл **`rewind_report.txt`**, скопируйте текст и отправьте в чат! 🎉

---

## 📂 Структура файлов
* `analyze_chat.py` — Логика парсинга и анализа данных.
* `generate_report.py` — Форматирование итогового отчета.
* `requirements.txt` — Список необходимых библиотек (`pandas`).
* `.gitignore` — Настройки git, чтобы случайно не выложить вашу переписку в интернет.